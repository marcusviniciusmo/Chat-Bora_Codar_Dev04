import { SendMocks } from "../../types/Send";
import SendIcon from '../../assets/sendIcon.png';

export const MockedData: SendMocks = {
  typeField: 'text',
  placeholder: 'Digite sua mensagem',
  buttonIcon: SendIcon,
  buttonTitle: 'Send'
};