import styled from "styled-components";

export const InputField = styled.input`
  width: 35rem;
  height: 3rem;
  padding-inline: 1rem;
  border: none;
  border-radius: 2rem;

  &::placeholder {
    opacity: 0.8;
  }
`;