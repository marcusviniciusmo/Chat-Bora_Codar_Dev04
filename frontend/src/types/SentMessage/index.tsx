export interface SentMessageMocks {
  classNameContact: string;
  classNameUser: string;
  labelUser: string;
};

export interface SentMessageProps {
  sender?: string;
  time: string;
  message: string;
};